{
  "name": "cb_web_test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "debug": "TEST_DIR=logs mocha -t 0 --inspect --debug-brk dev/",
    "test-dev": "TEST_DIR=logs mocha -t 0 dev/"
  },
  "devDependencies": {},
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0"
  }
}
